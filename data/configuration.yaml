data:
  lookback_days: 20  # Number of days to fetch historical data
  pairs:
    - symbol: "BTCUSDT"
      contract_type: "PERPETUAL"
      timeframes:
        - interval: "1D"
        # - interval: "1h"
