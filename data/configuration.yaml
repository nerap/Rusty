data:
  lookback_days: 1  # Number of days to fetch historical data
  pairs:
    - symbol: "BTCUSDT"
      contract_type: "PERPETUAL"
      timeframes:
        - interval: "5m"
        # - interval: "1h"
