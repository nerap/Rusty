data:
  lookback_days: 2  # Number of days to fetch historical data
  pairs:
    - symbol: "BTCUSDT"
      contract_type: "PERPETUAL"
      timeframes:
        - interval: "3m"
#        - interval: "1h"
